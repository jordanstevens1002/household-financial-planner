# The Python base image's embedded third-party SBOM retains superseded package
# versions after pip upgrades. The containers job verifies the versions installed
# in the final filesystem before suppressing these two stale SBOM records.
GHSA-6v7p-g79w-8964
CVE-2025-47273
